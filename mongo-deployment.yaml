apiVersion: apps/v1
kind: Deployment
metadata:
  name: hrapp-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hrapp-mongo
  template:
    metadata:
      labels:
        app: hrapp-mongo
    spec:
      containers:
      - name: mongo
        image: mongo:6.0
        ports:
        - containerPort: 27017
        volumeMounts:
        - name: mongo-data
          mountPath: /data/db
      volumes:
      - name: mongo-data
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: hrapp-mongo-service
spec:
  selector:
    app: hrapp-mongo
  ports:
  - port: 27017
    targetPort: 27017
  type: ClusterIP